<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ga.eatup.user.mapper.UserOrderMapper">

	<!-- <select id="orderList" parameterType="map" resultType="ga.eatup.user.domain.OrderVO"> 
		select * from tbl_order where ono > 0 and sno = #{sno} and del='N' order 
		by ono desc, regdate desc </select> -->


	<insert id="insert" useGeneratedKeys="true" keyProperty="tid">
		insert
		into gorany.order_info(tid, mno, quantity) value(tid, #{mno}, #{quantity})
	</insert>


</mapper>